/* Endereço da API. Em produção, troque para a URL do backend publicado
   (ex.: o serviço da Render) — é a única linha que precisa mudar por
   ambiente. Em desenvolvimento local, rode `npm start` dentro de server/
   e deixe o valor abaixo. */
window.BOLOS_API_BASE = "http://127.0.0.1:3000/api";
